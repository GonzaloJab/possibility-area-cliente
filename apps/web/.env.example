# API base URL. Leave empty in local dev to use `/api` (Vite proxy). This file is not used on Render — set VITE_API_URL on the Render *web* service so the Docker build bakes in your public API URL (https://…).
# Direct local calls: http://127.0.0.1:4000
VITE_API_URL=

