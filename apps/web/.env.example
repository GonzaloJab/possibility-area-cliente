# API base URL. Leave empty in dev to use `/api` (Vite → 127.0.0.1:4000; avoids localhost IPv6 issues on Windows).
# Or set e.g. http://127.0.0.1:4000 for direct calls. Production builds should set the real API origin.
VITE_API_URL=

