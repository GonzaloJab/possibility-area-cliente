# Blank in dev = `/api` via Vite proxy to 127.0.0.1:4000. Production Docker/Render sets a full API URL at build time.
VITE_API_URL=
# Client-facing app URL (impersonation / “open as client”)
VITE_CLIENT_URL=http://localhost:5173
